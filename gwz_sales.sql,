SELECT 
  date_date,
  ROUND(SUM(turnover), 2) AS daily_turnover,
  ROUND(SUM(purchase_cost), 2) AS total_purchase_cost
FROM 
  `fonksiyonlar.fonk.gwz_sales_17_copy`
GROUP BY 
  date_date
ORDER BY 
  date_date DESC;
